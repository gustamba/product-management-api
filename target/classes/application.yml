server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/stock
    username: postgres
    password: example

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
